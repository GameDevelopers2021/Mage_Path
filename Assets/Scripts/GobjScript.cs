using UnityEngine;

public class GobjScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("OK");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
